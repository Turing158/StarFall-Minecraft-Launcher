<ListView x:Class="StarFallMC.Component.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StarFallMC.Component"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" 
             Background="#00ffffff"
             BorderThickness="0"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType">
    <ListView.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Name="Main" Background="{Binding ItemBackground, RelativeSource={RelativeSource AncestorType=local:NavigationBar}}" 
                                Margin="2 0"
                                RenderTransformOrigin="0.5,0.5"
                                ClipToBounds="True" CornerRadius="10" >
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <ContentPresenter.Content>
                                        <TextBlock Text="{TemplateBinding Content}"
                                                   Margin="12 5"
                                                   FontSize="{Binding ItemFontSize, RelativeSource={RelativeSource AncestorType=local:NavigationBar}}" 
                                                   Foreground="{Binding ItemForeground, RelativeSource={RelativeSource AncestorType=local:NavigationBar}}" />
                                    </ContentPresenter.Content>
                                </ContentPresenter>
                                <Border Name="Active" Background="{Binding ItemForeground, RelativeSource={RelativeSource AncestorType=local:NavigationBar}}" 
                                        CornerRadius="8" Opacity="0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ContentPresenter.Content>
                                            <TextBlock Text="{TemplateBinding Content}"
                                                       Margin="12 5"
                                                       FontSize="{Binding ItemFontSize, RelativeSource={RelativeSource AncestorType=local:NavigationBar}}" 
                                                       Foreground="{Binding ItemHoverColor, RelativeSource={RelativeSource AncestorType=local:NavigationBar}}" />
                                        </ContentPresenter.Content>
                                    </ContentPresenter>
                                </Border>
                            </Grid>
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"></ScaleTransform>
                            </Border.RenderTransform>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Active" Storyboard.TargetProperty="Opacity"
                                                             To="1" Duration="0:0:0.2"></DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Active" Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.2"></DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <EventTrigger RoutedEvent="PreviewMouseLeftButtonDown">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Main" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="0.9" Duration="0:0:0.1" EasingFunction="{StaticResource CubicEase}"></DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="Main" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="0.9" Duration="0:0:0.1" EasingFunction="{StaticResource CubicEase}"></DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="PreviewMouseLeftButtonUp">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Main" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1" Duration="0:0:0.1" EasingFunction="{StaticResource CubicEase}"></DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="Main" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1" Duration="0:0:0.1" EasingFunction="{StaticResource CubicEase}"></DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Main" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1" Duration="0:0:0.1" EasingFunction="{StaticResource CubicEase}"></DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="Main" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1" Duration="0:0:0.1" EasingFunction="{StaticResource CubicEase}"></DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ListView.Resources>
    <ListView.ItemsPanel>
        <ItemsPanelTemplate>
            <VirtualizingStackPanel Orientation="Horizontal"></VirtualizingStackPanel>
        </ItemsPanelTemplate>
    </ListView.ItemsPanel>
</ListView>
