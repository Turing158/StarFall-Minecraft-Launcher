<UserControl x:Class="StarFallMC.Component.MaskControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StarFallMC.Component"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Name="Main">
    <UserControl.Resources>
        <Storyboard x:Key="MaskControlShow" TargetName="Main" TargetProperty="Opacity">
            <DoubleAnimation To="1" Duration="0:0:0.15"></DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="MaskControlHide" TargetName="Main" TargetProperty="Opacity">
            <DoubleAnimation To="0" Duration="0:0:0.15"></DoubleAnimation>
        </Storyboard>
        <Style TargetType="local:MaskControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="local:MaskControl">
                        <Grid>
                            <Border MouseLeftButtonDown="Mask_OnMouseLeftButtonDown" Background="{TemplateBinding MaskColor}" Opacity="{TemplateBinding MaskOpacity}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></Border>
                            <ContentPresenter></ContentPresenter>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
</UserControl>
